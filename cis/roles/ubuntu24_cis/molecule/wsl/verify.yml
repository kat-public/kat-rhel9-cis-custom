---

- name: Verify
  hosts: all
  gather_facts: false
  become: true

  tasks:
    - name: Verify | Check role completed successfully
      ansible.builtin.debug:
        msg: "UBUNTU24-CIS role applied successfully"
